import 'package:flutter/material.dart';

class GoogleMapsHomePage extends StatelessWidget {
  const GoogleMapsHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
