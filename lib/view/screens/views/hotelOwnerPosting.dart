import 'package:flutter/material.dart';

class HotelOwnerPosting extends StatelessWidget {
  const HotelOwnerPosting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Hotel Owner Posting"),
      ),
    );
  }
}
